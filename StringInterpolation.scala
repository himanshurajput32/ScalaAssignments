object StringInterpolation extends App{
val list =List(10,20,30,40)//created a new list
var count:Int=0  //Count for index 
for(ls<-list){ 
              println(s"$count = $ls")
              count+=1
             }
                                      }
